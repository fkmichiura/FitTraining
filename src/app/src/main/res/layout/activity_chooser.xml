<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/exercise_fragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_training2"
    tools:context="com.fkmichiura.project.fittraining.Activities.ChooserActivity">

    <Button
        android:id="@+id/confirm_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="52dp"
        android:background="@drawable/bg_rounded_button"
        android:text="@string/confirm_exercises" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/activity_chooser_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/confirm_button"
        android:scrollbars="vertical" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/activity_chooser_container"
        android:background="@android:color/black">

    </FrameLayout>

</RelativeLayout>
